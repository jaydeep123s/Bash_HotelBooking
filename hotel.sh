#!/bin/bash

. dategeneration.sh

file=$(pwd)/hotels
if [ -d "$file" ]; then 
    echo "Exist"
else
    echo "Does not exist"
    mkdir hotels
fi

cd hotels
choice=1
while (( choice ));do
    echo "Name :"
    read name
    echo "Location :"
    read location
    echo "Short description:"
    read description
    echo "No. of rooms:"
    read rooms

    str=$(echo $name | md5sum)
    id=${str:2:8}
    
    file=$(pwd)/$id.hotel
    #echo $file
    if [ -f "$file" ]; then
        echo "Hotel already exists "
    else
        echo "name:$name" >>$id.hotel
        echo "location:$location" >>$id.hotel
        echo "description:$description" >>$id.hotel
        echo "rooms:$rooms" >>$id.hotel
        generate_Date $rooms $id

        echo "Hotel added ...."
    fi

    echo "Do you want to add another hotel ?"
    read choice
done

cd ..






















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































